<div class="index-menu breadcrumbs">
  <%= link_to "Home", controller: "lists", action: "home", class: "breadcrumbs-crumb" %>
  <span class="breadcrumbs-seperator">></span>
  <%= link_to element.type_for_display.titleize, controller: element.class.to_s.tableize, action: "index", class: "breadcrumbs-crumb" %>
  <span class="breadcrumbs-seperator">></span>
  <%= link_to element.name[0], controller: element.class.to_s.tableize, action: "letter_index", letter: element.name[0], class: "breadcrumbs-crumb" %>
  <span class="breadcrumbs-seperator">></span>
  <%= link_to element.name, element.url, class: "breadcrumbs-crumb" %>
</div>
