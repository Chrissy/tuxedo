<% cache(cache_key(@last, "more-#{@page}")) do %>
  <%= render 'list_elements', page: @page, elements: @list_elements, start_index: @pagination_start, pagination_end: @pagination_end, last_page: @last_page %>
<% end %>
