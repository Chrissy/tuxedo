<% cache(index_letter_cache_key(@elements, Component, params[:letter])) do %>
  <%= render 'shared/index_body', :controller => :components, :elements => @elements %>
  <%= render 'shared/index_alphabet_menu', :controller => :components, :title => 'Ingredients A-Z' %>
  <%= render 'shared/breadcrumbs_for_letter', :element => Component.new, :letter => params[:letter] %>
<% end %>
